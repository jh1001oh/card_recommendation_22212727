# Design

### BeneFit

<img src="https://github.com/jh1001oh/conceptualization--22212727-/blob/main/BeneFIT%20Logo.png?raw=true" width="400" height="400"/>

| **Student No.** | 22212727 |
| --- | --- |
| **Name** | 오지후 |
| **E-Mail** | nyeong22126@daum.net |
 
---
 

## [ Revision history ]

| **Revision date** | **Version** | **Description** | **Author** |
| --- | --- | --- | --- |
| 22/03/26 | v.1.00 | First Draft | 오지후 |
| 27/04/26 | v.1.00 |  | 오지후 |
| 02/06/26 |           | 오지후 |
---

## = Contents =
1. Introduction
2. Class diagram
3. Sequence diagram
4. State machine diagram
5. Implementation requirements
6. Glossary
7. References

---

# 1. Introduction
   
## 1) Executive Summary
   
 현대 사회에서 신용카드는 단순한 결제 수단을 넘어 다양한 혜택과 서비스를 제공하는 핵심 금융 도구가 되었다. 그러나 다양하게 유통되고 있는 카드 중 자신의 소비 패턴에 맞는 카드를 직접 비교하고 선택하는 것은 일반 소비자에게 매우 어렵고 시간이 많이 걸리는 일이다. 지난 개념화 문서에서 정의한 요구사항을 바탕으로 하여 사용자가 카테고리별 월평균 지출액을 입력하면 최적의 신용카드를 자동으로 추천해주는 앱 'BeneFit'의 분석 단계를 기술한다.
 시스템이 무엇을 하는가(what)에 초점을 맞추어 Use case analysis, Domain analysis, User Interface prototype을 다룬다. 각 장에서는 UML을 활용한 분석 결과와 그 의도를 함께 기술하였다.

## 2) Business Goals
   
 BeneFit은 사용자가 자신의 소비 습관에 맞는 신용카드를 쉽고 빠르게 찾을 수 있도록 지원하는 것을 목적으로 한다. 외식, 쇼핑, 통신, OTT, 주유, 편의점 등 카테고리별 지출액을 입력하면 카드별 혜택을 자동으로 계산하여 예상 할인·적립 금액이 높은 순서대로 추천 결과를 제공한다. 단순한 정보 제공을 넘어 사용자가 실질적인 경제적 이익을 극대화할 수 있도록 맞춤형 솔루션을 제시하는 것이 핵심 목표이다.

## 3) Technical Goals
   
 BeneFit은 콘솔 앱 형태로 제공되며 User와 Administrator가 서버의 카드 혜택 DB와 통신하는 구조로 설계한다. 카드 혜택 계산 알고리즘은 전월 실적 조건, 카테고리별 할인·적립률, 월 한도 등의 조건들을 정확하게 처리해야 하며 추천 결과는 입력 완료 후 3초 이내로 화면에 출력되어야 한다.

---

# 2. Class diagram

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/Class%20diagram.png"/>

아래는 위의 Class Diagram에서 표현한 Class들에 대한 설명이다.

### 1) Main

**(1) Methods**

- `+main(args: String[]): void` : 프로그램의 진입점이 되 메소드이다. AuthService, RecommendEngine, Administrator를 호출하여 전체 시스템 흐름을 제어한다.

---

### 2) AuthService

회원가입, 로그인, 로그아웃을 처리하는 클래스이다. MemberRepository에 의존하여 이메일 중복 확인 후 회원가입을 수행하고 로그인 시 User 객체를 반환한다.

**(1) Attributes**

- `-memberDB: MemberRepository` : 회원 데이터를 저장하고 조회하는 MemberRepository 객체이다.

**(2) Methods**

- `+register(email: String, password: String, name: String): boolean` : 이메일 중복 여부를 확인한 후 새 회원을 등록하는 메소드이다. 성공 시 true, 실패 시 false를 반환한다.
- `+login(email: String, password: String): User` : 이메일과 비밀번호를 검증하여 로그인을 수행하는 메소드이다. 인증 성공 시 User 객체를 반환하고 실패 시 null을 반환한다.
- `+logout(): void` : 현재 로그인된 사용자의 세션을 종료하는 메소드이다.

---

### 3) MemberRepository

전체 회원 목록을 `List<User>`로 보유하고 파일에 저장하여 프로그램 종료 후에도 유지한다.

**(1) Attributes**

- `-members: List<User>` : 등록된 전체 회원 목록을 저장하는 리스트이다.

**(2) Methods**

- `+save(u: User): void` : 새로운 User 객체를 회원 목록에 추가하고 파일에 저장하는 메소드이다.
- `+findByEmail(email: String): User` : 이메일로 회원을 검색하여 해당 User 객체를 반환하는 메소드이다. 존재하지 않으면 null을 반환한다.
- `+findAll(): List<User>` : 전체 회원 목록을 반환하는 메소드이다.
- `+delete(email: String): boolean` : 이메일에 해당하는 회원을 삭제하는 메소드이다. 성공 시 true를 반환한다.
- `+update(u: User): void` : 기존 회원 정보를 수정된 User 객체로 업데이트하는 메소드이다.

---

### 4) User

회원 정보를 저장하는 클래스이다.

**(1) Attributes**

- `-email: String` : 로그인 및 회원 식별에 사용되는 이메일 주소이다.
- `-password: String` : 로그인 인증에 사용되는 비밀번호이다.
- `-name: String` : 사용자의 이름이다.
- `-role: String` : 사용자의 권한을 나타내는 필드이다. "user" 또는 "admin" 값을 가진다.
- `-spendingData: SpendingData` : 사용자의 카테고리별 월평균 지출액을 저장하는 객체이다.

**(2) Methods**

- `+User(email: String, password: String, name: String, role: String): void` : User 객체를 생성하는 생성자이다.
- `+getEmail(): String` : 이메일 값을 반환하는 메소드이다.
- `+getName(): String` : 이름 값을 반환하는 메소드이다.
- `+getRole(): String` : 권한 값을 반환하는 메소드이다. 관리자 여부 확인 시 사용된다.
- `+getSpendingData(): SpendingData` : 사용자의 지출 데이터 객체를 반환하는 메소드이다.
- `+setSpendingData(sd: SpendingData): void` : 사용자의 지출 데이터를 설정하는 메소드이다.

---

### 5) Administrator

User를 상속(extends)하는 관리자 클래스이다. CardDB에 의존하여 카드 정보를 관리한다.

**(1) Attributes**

- `-adminEmail: String` : 관리자 계정의 이메일이다.

**(2) Methods**

- `+Administrator(email: String, password: String, name: String): void` : Administrator 객체를 생성하는 생성자이다.
- `+addCard(c: CardInfo): void` : 새로운 카드 정보를 CardDB에 추가하는 메소드이다.
- `+updateCard(c: CardInfo): void` : 기존 카드 정보를 수정하는 메소드이다.
- `+deleteCard(cardId: String): boolean` : 카드 ID에 해당하는 카드를 삭제하는 메소드이다. 성공 시 true를 반환한다.
- `+listAllCards(): void` : 현재 등록된 전체 카드 목록을 출력하는 메소드이다.

---

### 6) SpendingData

사용자의 카테고리별 월평균 지출액을 저장하는 클래스이다. 외식(`food`), 쇼핑(`shopping`), 통신(`telecom`), OTT(`ott`), 주유(`fuel`), 편의점(`convenience`) 6개 카테고리를 int로 관리한다. 

**(1) Attributes**

- `-food: int` : 외식 카테고리의 월평균 지출액이다. (단위: 원)
- `-shopping: int` : 쇼핑 카테고리의 월평균 지출액이다. (단위: 원)
- `-telecom: int` : 통신 카테고리의 월평균 지출액이다. (단위: 원)
- `-ott: int` : OTT 카테고리의 월평균 지출액이다. (단위: 원)
- `-fuel: int` : 주유 카테고리의 월평균 지출액이다. (단위: 원)
- `-convenience: int` : 편의점 카테고리의 월평균 지출액이다. (단위: 원)

**(2) Methods**

- `+SpendingData(): void` : SpendingData 객체를 생성하는 기본 생성자이다.
- `+setCategory(category: String, amount: int): void` : 특정 카테고리의 지출액을 설정하는 메소드이다.
- `+getAmount(category: String): int` : 특정 카테고리의 지출액을 반환하는 메소드이다.
- `+getTotalMonthly(): int` : 전체 카테고리의 월 지출 합계를 반환하는 메소드이다.
- `+toMap(): Map<String, Integer>` : 카테고리별 지출액을 Map 형태로 변환하여 반환하는 메소드이다. RecommendEngine에서 혜택 계산 시 사용된다.

---

### 7) CardDB

전체 카드 정보를 `List<CardInfo>`로 보유하는 데이터베이스 역할의 클래스이다.

**(1) Attributes**

- `-cards: List<CardInfo>` : 전체 카드 정보를 저장하는 리스트이다.

**(2) Methods**

- `+loadAll(): void` : 파일에서 전체 카드 데이터를 불러와 cards 리스트에 저장하는 메소드이다.
- `+findAll(): List<CardInfo>` : 전체 카드 목록을 반환하는 메소드이다.
- `+findById(cardId: String): CardInfo` : 카드 ID로 특정 카드 정보를 반환하는 메소드이다.
- `+save(c: CardInfo): void` : 새로운 CardInfo 객체를 저장하는 메소드이다.
- `+update(c: CardInfo): void` : 기존 카드 정보를 수정된 CardInfo 객체로 업데이트하는 메소드이다.
- `+delete(cardId: String): boolean` : 카드 ID에 해당하는 카드를 삭제하는 메소드이다. 성공 시 true를 반환한다.

---

### 8) CardInfo

신용카드 정보를 나타내는 클래스이다.

**(1) Attributes**

- `-cardId: String` : 카드의 고유 식별자이다.
- `-cardName: String` : 카드의 이름이다.
- `-company: String` : 카드를 발급한 카드사 이름이다.
- `-annualFee: int` : 카드의 연회비이다. (단위: 원)
- `-minPerformance: int` : 카드 혜택을 받기 위한 전월 실적 최소 조건이다. (단위: 원)
- `-benefits: List<BenefitCondition>` : 해당 카드의 카테고리별 혜택 조건 목록이다.

**(2) Methods**

- `+CardInfo(cardId, cardName, company, annualFee, minPerformance): void` : CardInfo 객체를 생성하는 생성자이다.
- `+addBenefit(bc: BenefitCondition): void` : 카드에 혜택 조건을 추가하는 메소드이다.
- `+getBenefits(): List<BenefitCondition>` : 해당 카드의 전체 혜택 조건 목록을 반환하는 메소드이다.
- `+getCardName(): String` : 카드명을 반환하는 메소드이다.
- `+getAnnualFee(): int` : 연회비를 반환하는 메소드이다.
- `+getMinPerformance(): int` : 전월 실적 조건을 반환하는 메소드이다.
- `+displayDetail(): void` : 카드의 상세 정보(카드명, 카드사, 연회비, 전월 실적, 혜택 목록)를 출력하는 메소드이다.

---

### 9) BenefitCondition

CardInfo와 composition 관계를 가지는 혜택 조건 클래스이다.

**(1) Attributes**

- `-category: String` : 혜택이 적용되는 카테고리 이름이다. (예: food, shopping, telecom 등)
- `-discountRate: double` : 해당 카테고리에서 적용되는 할인율이다. (0.0 ~ 1.0)
- `-cashbackRate: double` : 해당 카테고리에서 적용되는 적립률이다. (0.0 ~ 1.0)
- `-monthlyLimit: int` : 한 달 동안 받을 수 있는 최대 혜택 금액이다. (단위: 원)

**(2) Methods**

- `+BenefitCondition(category, discountRate, cashbackRate, monthlyLimit): void` : BenefitCondition 객체를 생성하는 생성자이다.
- `+calculateBenefit(spent: int): int` : 실제 지출액을 입력받아 해당 카테고리의 예상 혜택 금액을 계산하여 반환하는 메소드이다. monthlyLimit을 초과하지 않도록 처리한다.
- `+getCategory(): String` : 카테고리 이름을 반환하는 메소드이다.
- `+getDiscountRate(): double` : 할인율을 반환하는 메소드이다.
- `+getCashbackRate(): double` : 적립률을 반환하는 메소드이다.

---

### 10) RecommendEngine

핵심 추천 알고리즘 클래스이다.

**(1) Attributes**

- `-cardDB: CardDB` : 전체 카드 정보를 조회하기 위해 의존하는 CardDB 객체이다.

**(2) Methods**

- `+recommend(sd: SpendingData, topN: int): List<RecommendResult>` : 사용자의 지출 데이터를 기반으로 전체 카드의 예상 혜택을 계산하고, 혜택이 높은 순서대로 상위 N개의 RecommendResult를 반환하는 메소드이다.
- `-calcTotalBenefit(card: CardInfo, sd: SpendingData): int` : 특정 카드와 사용자의 지출 데이터를 입력받아 카테고리별 혜택을 합산한 총 예상 혜택 금액을 계산하는 내부 메소드이다.
- `-isEligible(card: CardInfo, sd: SpendingData): boolean` : 사용자의 총 지출액이 카드의 전월 실적 조건을 충족하는지 확인하는 내부 메소드이다. 충족하지 않으면 해당 카드를 추천 대상에서 제외한다.

---

### 11) RecommendResult

RecommendEngine의 추천 결과를 저장하는 클래스이다. 

**(1) Attributes**

- `-card: CardInfo` : 추천된 카드의 정보 객체이다.
- `-estimatedMonthlyBenefit: int` : 사용자의 지출 패턴 기준으로 계산된 예상 월 혜택 금액이다. (단위: 원)
- `-pickingRate: double` : 총 지출 대비 혜택 금액의 비율이다. (혜택 금액 / 총 지출액)
- `-benefitBreakdown: Map<String, Integer>` : 카테고리별 예상 혜택 금액을 항목별로 저장한 Map이다.

**(2) Methods**

- `+RecommendResult(card, benefit, pickingRate, breakdown): void` : RecommendResult 객체를 생성하는 생성자이다.
- `+getCard(): CardInfo` : 추천된 카드의 CardInfo 객체를 반환하는 메소드이다.
- `+getEstimatedMonthlyBenefit(): int` : 예상 월 혜택 금액을 반환하는 메소드이다.
- `+getPickingRate(): double` : 피킹률을 반환하는 메소드이다.
- `+displaySummary(): void` : 카드명, 예상 월 혜택 금액, 피킹률을 화면에 출력하는 메소드이다.

---

### 12) CompareResult

사용자가 선택한 2~3개의 카드를 비교한 결과를 저장하는 클래스이다. 

**(1) Attributes**

- `-selectedCards: List<CardInfo>` : 사용자가 비교를 위해 선택한 2~3개의 카드 목록이다.
- `-comparedData: Map<String, Integer>` : 카드별 카테고리별 혜택 금액을 비교한 데이터이다.

**(2) Methods**

- `+CompareResult(cards: List<CardInfo>, sd: SpendingData): void` : 선택된 카드 목록과 지출 데이터를 받아 비교 결과를 생성하는 생성자이다.
- `+display(): void` : 선택된 카드들의 연회비와 카테고리별 혜택을 나란히 비교 표 형태로 출력하는 메소드이다.
- `+getComparedData(): Map<String, Integer>` : 비교 데이터를 반환하는 메소드이다.


---

# 3. Sequence diagram

아래에 나오는 그림들은 Analysis에서 정의한 Use Case들을 Sequence Diagram으로 표현한 그림들이다.

## 1) Register Member

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%ED%9A%8C%EC%9B%90%EA%B0%80%EC%9E%85Sequence.png"/>

위의 그림은 사용자가 회원가입을 수행할 때를 표현한 Sequence Diagram이다. 사용자가 시작 화면에서 회원가입을 선택하면 Main이 AuthService의 `register()` 메소드를 호출한다. AuthService는 MemberRepository의 `findByEmail()`을 통해 이메일 중복 여부를 확인한다. 중복이 없으면 MemberRepository의 `save()`를 호출하여 계정을 저장하고 성공 메시지를 출력한다. 비밀번호 불일치 또는 이메일 중복의 경우 각각 오류 메시지를 출력하고 입력 화면으로 돌아간다.

## 2) Login/Logout

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%EB%A1%9C%EA%B7%B8%EC%9D%B8%2C%EB%A1%9C%EA%B7%B8%EC%95%84%EC%9B%83Sequence.png"/>

위의 그림은 사용자가 로그인 및 로그아웃을 수행할 때를 표현한 Sequence Diagram이다. 사용자가 이메일과 비밀번호를 입력하면 Main이 AuthService의 `login()` 메소드를 호출한다. AuthService는 MemberRepository의 `findByEmail()`로 회원 정보를 조회하여 인증을 수행한다. 인증에 성공하면 User 객체를 반환하고 지출 정보 입력 화면으로 이동한다. 인증에 실패하면 오류 메시지를 출력하고 입력칸을 초기화한다. 로그아웃 시에는 `logout()` 메소드를 호출하여 세션을 종료하고 시작 화면으로 돌아간다.

## 3) Input Spending Data/Edit Spending Data

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%EC%A7%80%EC%B6%9C%EC%A0%95%EB%B3%B4%EC%9E%85%EB%A0%A5%EC%88%98%EC%A0%95Sequence.png"/>

위의 그림은 사용자가 지출 정보를 입력하거나 수정할 때를 표현한 Sequence Diagram이다. 로그인 성공 후 시스템은 기존에 저장된 SpendingData를 불러와 화면에 표시한다. 사용자는 외식, 쇼핑, 통신, OTT, 주유, 편의점 카테고리별로 월평균 지출액을 입력하거나 수정한다. 입력이 완료되면 Main이 User의 `setSpendingData()`를 호출하여 데이터를 저장하고, 저장이 완료되면 카드 추천 결과 화면으로 자동 이동한다. 모든 카테고리에 0원을 입력한 경우에는 오류 메시지를 출력하고 재입력을 요청한다.

## 4) View Recommendation Result

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%EC%B9%B4%EB%93%9C%EC%B6%94%EC%B2%9C%EA%B2%B0%EA%B3%BC%EC%A1%B0%ED%9A%8CSequence.png"/>

위의 그림은 시스템이 카드 추천 결과를 계산하고 출력할 때를 표현한 Sequence Diagram이다. 지출 정보 저장이 완료되면 Main이 RecommendEngine의 `recommend()` 메소드를 호출한다. RecommendEngine은 CardDB의 `findAll()`로 전체 카드 목록을 가져온 뒤, 각 카드에 대해 `isEligible()`로 전월 실적 조건을 확인하고 `calcTotalBenefit()`으로 예상 혜택 금액을 계산한다. 계산된 결과를 내림차순으로 정렬하여 RecommendResult 목록으로 반환하고, 카드명·예상 월 혜택 금액·연회비를 화면에 출력한다. 혜택 조건을 충족하는 카드가 없는 경우에는 안내 메시지를 출력한다.

## 5) View Card Detail

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%EC%B9%B4%EB%93%9C%EC%83%81%EC%84%B8%EC%A0%95%EB%B3%B4%ED%99%95%EC%9D%B8Sequence.png"/>

위의 그림은 사용자가 추천 결과에서 특정 카드를 선택하여 상세 정보를 확인할 때를 표현한 Sequence Diagram이다. 사용자가 추천 결과 화면에서 카드 번호를 선택하면 Main이 CardDB의 `findById()`를 호출하여 해당 카드의 상세 정보를 가져온다. 조회에 성공하면 CardInfo의 `displayDetail()`을 통해 주요 혜택, 전월 실적 조건, 월 예상 혜택 한도, 연회비를 화면에 출력한다. 조회에 실패한 경우에는 오류 메시지를 출력하고 추천 결과 화면으로 돌아간다.

## 6) Compare Cards

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%EC%B9%B4%EB%93%9C%EB%B9%84%EA%B5%90Sequence.png"/>

위의 그림은 사용자가 2~3개의 카드를 선택하여 혜택을 비교할 때를 표현한 Sequence Diagram이다. 사용자가 추천 결과 화면에서 비교할 카드를 선택하면 Main이 선택된 카드들의 정보를 CardDB의 `findById()`로 가져온다. 이후 CompareResult 객체를 생성하고 `display()`를 호출하여 연회비와 카테고리별 혜택을 나란히 비교표 형태로 출력한다. 카드를 1개만 선택한 경우에는 2개 이상 선택하도록 안내 메시지를 출력하고, 4개 이상 선택하려는 경우에는 최대 3개까지 선택 가능하다는 안내 메시지를 출력한다.

## 7) Manage Card Info

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/%EC%B9%B4%EB%93%9CDB%EA%B4%80%EB%A6%ACSequence.png"/>

위의 그림은 관리자가 카드 정보를 관리할 때를 표현한 Sequence Diagram이다. 관리자 계정으로 로그인하면 Main이 Administrator의 `listAllCards()`를 호출하여 현재 등록된 전체 카드 목록을 출력한다. 관리자는 신규 카드 추가, 기존 카드 수정, 카드 삭제 중 하나를 선택한다. 추가 시에는 `addCard()`를, 수정 시에는 `updateCard()`를, 삭제 시에는 `deleteCard()`를 호출하여 CardDB에 즉시 반영한다. 관리자 권한이 없는 사용자가 접근을 시도할 경우 접근 권한 없음 메시지를 출력하고 메인 화면으로 이동한다.

---

# 4. State machine diagram

<img src="https://github.com/jh1001oh/card_recommendation_22212727/blob/main/State%20machine%20diagram.png"/>

위의 그림은 BeneFIT 시스템의 State machine diagram을 표현한 그림이다.

아래는 위의 State machine diagram에 나온 각 State들에 대한 설명이다.

| **State** | **Explanation** |
| --- | --- |
| Launch System | 시스템이 실행되어 시작 화면이 표시된 상태이다. 사용자는 로그인 또는 회원가입을 선택할 수 있다. |
| Register Member | 사용자가 이메일, 비밀번호, 비밀번호 확인, 닉네임을 입력하는 상태이다. |
| Wait Register Result | 입력한 회원 정보의 유효성을 검증하는 상태이다. 비밀번호 불일치 또는 이메일 중복의 경우 Register Member 상태로 돌아가고, 성공 시 Launch System 상태로 이동한다. |
| Wait Login Input | 사용자가 이메일과 비밀번호를 입력하는 상태이다. |
| Wait Login Result | 입력한 이메일과 비밀번호를 서버에 저장된 회원 정보와 비교하여 인증을 수행하는 상태이다. 인증 성공 시 Input Spending Data 상태로, 실패 시 Wait Login Input 상태로 돌아간다. |
| Input Spending Data | 로그인 성공 후 사용자가 카테고리별 월평균 지출액을 입력하거나 수정하는 상태이다. 외식, 쇼핑, 통신, OTT, 주유, 편의점 6개 카테고리의 금액을 입력한다. |
| View Recommendation Result | 시스템이 입력된 지출 데이터와 카드 혜택 DB를 비교하여 계산한 추천 결과를 출력하는 상태이다. 예상 월 혜택 금액이 높은 순서대로 카드 목록이 표시된다. |
| View Card Detail | 사용자가 추천 결과에서 특정 카드를 선택하여 주요 혜택, 전월 실적 조건, 월 예상 혜택 한도, 연회비 등 상세 정보를 확인하는 상태이다. |
| Compare Cards | 사용자가 2~3개의 카드를 선택하여 연회비와 카테고리별 혜택을 나란히 비교표 형태로 확인하는 상태이다. |
| Manage Card Info | 관리자 계정으로 로그인한 경우 접근 가능한 상태이다. 신규 카드 추가, 기존 카드 수정, 카드 삭제를 수행하며 변경 내용은 CardDB에 즉시 반영된다. |

---

# 5. Implementation requirements

BeneFIT 시스템을 구동하기 위해 필요한 요구사항은 아래와 같다.

## 1) Hardware Requirements

| 항목 | 사양 |
| --- | --- |
| CPU | Intel Pentium IV 이상 |
| RAM | 1GB 이상 |
| HDD / SSD | 10GB 이상의 여유 공간 |

## 2) Software Requirements

| 항목 | 사양 |
| --- | --- |
| OS | Windows 7 이상 / macOS 10.13 이상 |
| 구현 언어 | Java (JDK 11 이상) |
| Network | 인터넷 연결 불필요 (로컬 파일 기반 동작) |

## 3) Nonfunctional Requirements

- 카드 추천 결과는 지출 정보 입력 완료 후 3초 이내에 화면에 출력되어야 한다.
- 회원 데이터 및 카드 데이터는 텍스트 파일(`.txt`)로 로컬에 저장되며, 프로그램 종료 후에도 유지되어야 한다.
- 관리자와 일반 사용자의 접근 권한은 `role` 필드로 구분되며, 권한 외의 기능에는 접근할 수 없어야 한다.
- 콘솔 앱 형태로 제공되며 별도의 서버나 데이터베이스 없이 동작한다.

---

# 6. Glossary

| **Terms** | **Description** |
| --- | --- |
| Class Diagram | 객체지향형 시스템 설계에서 클래스의 존재와 그들의 관계를 도식으로 정의한 것이다. 단일 클래스 다이어그램은 시스템의 클래스 구조를 보여준다. |
| Sequence Diagram | 객체들 사이의 상호작용을 시간 순서에 따라 표현한 다이어그램이다. 메소드 호출 흐름과 조건 분기를 시각적으로 나타낸다. |
| State Machine Diagram | 시스템의 동작을 상태와 전이로 설명하는 다이어그램이다. 시스템이 가질 수 있는 한정된 수의 상태와 각 상태 간의 전환 조건을 표현한다. |
| Attribute | 객체지향형 프로그래밍 언어에서 클래스의 멤버 변수를 가리킨다. |
| Method | 멤버 함수라고도 하며, 객체지향형 프로그래밍 언어에서 클래스 혹은 객체에 소속된 서브루틴을 가리킨다. |
| Composition | 한 클래스가 다른 클래스를 강하게 포함하는 관계로, 포함된 객체는 포함하는 객체 없이 독립적으로 존재할 수 없다. |
| 할인(Discount) | 결제 금액의 일정 비율 또는 금액을 카드사가 청구 금액에서 차감하는 혜택이다. |
| 적립(Cashback) | 결제 금액에 비례하여 포인트나 마일리지를 쌓아주는 혜택이다. |
| 전월 실적 | 카드 혜택을 받기 위해 직전 달에 해당 카드로 사용해야 하는 최소 결제 금액이다. |
| 피킹률(Picking Rate) | 카드로 결제한 금액 대비 실제로 돌려받은 혜택의 비율이다. |
| OTT(Over-The-Top) | 인터넷을 통해 제공되는 스트리밍 서비스이다. 넷플릭스, 티빙, 디즈니플러스 등이 해당된다. |

---

# 7. References

